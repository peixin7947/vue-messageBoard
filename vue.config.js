// vue.config.js
module.exports = {
    // 是否为保存的lint使用eslint-loader
    // 可选值： true | false | 'error'
    // 当设置为“error”时，lint错误会导致编译打包失败
    lintOnSave: true,
    // 输出构建的文件的地方
    outputDir: 'dist',
}
